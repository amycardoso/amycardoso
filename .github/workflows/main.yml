name: generate animation

on:
  schedule:
      # every 12 hours
    - cron: "0 0 * * *"

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v0.0.18
        with:
          github_user_name: amycardoso
